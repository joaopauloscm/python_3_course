#split(): string method used to break a single string into a list of smaller substrings 
# join(): merge the elements of an iterable (like a list, tuple, or set) into a single string

sentence = "Hello dear friend"
sentence_with_split = sentence.split()
print(sentence_with_split)

join_sentence = '_'.join("abc")
print (join_sentence)